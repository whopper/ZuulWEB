function modalpop(divid) {
  el = document.getElementById(divid);
  el.style.visibility = (el.style.visibility == "visible") ? "hidden": "visible";
}

function initcraft() {
  alert("This doesn't actually work quite yet...")
}
